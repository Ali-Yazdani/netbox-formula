# -*- coding: utf-8 -*-
# vim: ft=jinja

{#- Get the `tplroot` from `tpldir` #}
{%- set tplroot = tpldir.split('/')[0] %}
{#- Start imports as #}
# {%- import_yaml tplroot ~ "/defaults.yaml" as default_settings %}
#
# {% set defaults = default_settings %}
{%- set settings = salt['pillar.get']('netbox', default=defaults, merge=True) %}
